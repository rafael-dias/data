name: Convert Markdown

on: [push]

jobs:
  install:
    name: Convert Markdown
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v3

     - name: test
       run: python convert.py
       
   
